version: 2.1

workflows:
  build-deploy:
    jobs:
     - build

jobs:
  build:
    machine: 
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - run:
          name: Run Script
          command: |
             pwd
             ls -lh
             chmod +x sanity_test.sh
             bash sanity_test.sh
